import styled from "styled-components"
 
export default function SelectSeats(prop) {
 
  
 
   
 
 
   return (
   <>
    {/*
   <SeatSelect>
     <form type='checkbox'>
       {seats.map((n) => n.isAvailable ? <input type="checkbox" value={n.id}/> : <input disabled='true' type='checkbox'/>)}
    
     </form>
   </SeatSelect>
   */}
   </>
   );
 }
  const SeatSelect = styled.div `
  
 `
 
 const SeatButton = styled`
   background-color: gray;
   width: 15px;
   height: 15px;
   border-radius: 15px;
 `
 
